{
  "name": "@postphantom/shared",
  "version": "1.0.0",
  "description": "Shared types and interfaces for PostPhantom",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "fast-check": "^3.15.0",
    "@types/node": "^20.10.0"
  }
}